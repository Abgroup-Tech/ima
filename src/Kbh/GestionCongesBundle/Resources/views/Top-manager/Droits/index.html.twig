{% extends '::base.html.twig' %}

{% block main %}
    <h1>Droits list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Droitsacquisanneeencours</th>
                <th>Reliquatdroitsanterieur</th>
                <th>Cumuldroitsacquis</th>
                <th>Droitspris</th>
                <th>Totaldroitsaprendre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('droits_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.droitsAcquisAnneeEnCours }}</td>
                <td>{{ entity.reliquatDroitsAnterieur }}</td>
                <td>{{ entity.cumulDroitsAcquis }}</td>
                <td>{{ entity.droitsPris }}</td>
                <td>{{ entity.totalDroitsAprendre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('droits_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('droits_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('droits_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
