{% extends '::base-admin.html.twig' %}

{% block main %}
    <h1>TypeAbsence</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ entity.categorie }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typeabsence') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('typeabsence_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    
</ul>
{% endblock %}
