{% extends '::base-admin.html.twig' %}

{% block main %}
    <h1>Organigramme</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombreniveaux</th>
                <td>{{ entity.nombreNiveaux }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('organigramme') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('organigramme_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    
</ul>
{% endblock %}
