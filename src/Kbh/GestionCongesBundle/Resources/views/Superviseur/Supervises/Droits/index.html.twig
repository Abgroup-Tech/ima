{% extends '::base.html.twig' %}

{% block main %}
    <h1>Droits list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Salarié</th>
                <th>Cumul droits acquis</th>
                <th>Droits pris</th>
                <th>Total droits à prendre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{#{{ path('salarie_show', { 'id': entity.id }) }}#}">{{ entity.nomprenom }}</a></td>
                <td><a href="{{ path('droits_show', { 'id': entity.id }) }}">{{ entity.cumulDroitsAcquis }}</a></td>
                <td>{{ entity.droitsPris }}</td>
                <td>{{ entity.totalDroitsAprendre }}</td>
                <td>
                <ul>
                    {#<li>
                        <a href="{{ path('droits_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('droits_edit', { 'id': entity.id }) }}">edit</a>
                    </li>#}
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        {#<li>
            <a href="{{ path('droits_new') }}">
                Create a new entry
            </a>
        </li>#}
    </ul>
    {% endblock %}
