{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BaseDroits creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('basedroits') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
